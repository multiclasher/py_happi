language: python
jobs:
  include:
    - name: "Python 3.8 on Linux"
      os: linux
      dist: xenial
      language: python
      python: 3.8
      script:
        - sudo pip install tox
install: pip install --upgrade pip
script:
  - tox -e short-test
