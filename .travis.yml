language: python
jobs:
  include:
      python: 3.8
      os: linux
      arch: arm64
      before_install:
        - sudo pip install tox
install: pip install --upgrade pip
script:
  - tox -e short-test
